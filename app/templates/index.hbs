{{outlet}} {{header-navigation}}
<div class="row">
    <div class="small-12 columns">
        {{#each model as |aroma|}}
            {{#link-to "aromas.show" aroma}}
                <div class="callout">
                    <div class="row">
                        <div class="small-12 columns">
                            <h2>{{aroma.name}}</h2>
                        </div>
                    </div>
                </div>
            {{/link-to}}
        {{/each}}
    </div>
</div>